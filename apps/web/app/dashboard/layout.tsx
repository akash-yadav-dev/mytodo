// Dashboard layout — currently a pass-through.
// Add a dashboard-specific sidebar here when needed.
export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
